meta:
  environment: docker
  toolchain:
    name: docker
    version: latest

build:
  dockerfile: Dockerfile

run:
  persistenceMount: /data
  containerPort: 8000
  command: python
  args: -m parts_monitor.cli collect --source aodesf7
